/**
 * Moore's Voting Algorithm
 */

#include "leetc.h"

class Solution {
public:
    int majorityElement(vector<int> &nums) {
        int n = nums.size();
        int candidate = nums[0], cnt = 0;
        for(int i = 0; i < n; i++) {
            if(cnt == 0) {
            }
        }
    }
};
